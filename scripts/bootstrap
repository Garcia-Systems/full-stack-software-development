#!/bin/sh
set -eu
command -v docker >/dev/null 2>&1 || { echo 'Docker is required (Docker Desktop or Engine with Compose v2).' >&2; exit 1; }
docker compose version >/dev/null
[ -f .env ] || cp .env.example .env
echo 'Ready. Run: make up'
